const actionHandler = require('./action');


describe('action.js', () => {
    describe('', () => {
        const fn = actionHandler.setPath;
        it('should', () => {
            expect(1).toBe(1)
        });
    });

});
