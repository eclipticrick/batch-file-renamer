const steps = require('./steps');


describe('steps.js', () => {
    describe('', () => {
        const fn = steps.getStep;
        it('should', () => {
            expect(1).toBe(1)
        });
    });

});
